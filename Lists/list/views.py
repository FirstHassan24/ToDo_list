from django.shortcuts import render,redirect
#import the models:
from .models import List,Item
#import messages for error handling:
from django.contrib import messages
#2.make a function so the user can can create a category:


